"save": {
    "enabled": true,
    "every_n_seconds": null,
    "every_n_epochs": 50, #this is how often we save the model
    "epochs": [],
    "on_best_validation": false,
    "on_best_rollout_return": false,
    "on_best_rollout_success_rate": true
},
"epoch_every_n_steps": 100, #this is how often we generate the video
"validation_epoch_every_n_steps": 10, #this is the bar underneath
"env": null,